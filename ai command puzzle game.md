# [개발 프롬프트] AI 명령 학습 퍼즐 게임 — 가제 "정확히 말해줘"

> 이 문서는 웹 게임(HTML/CSS/JavaScript)을 만드는 코딩 AI에게 그대로 전달하기 위한 기획 명세서입니다.
> 아래 사양대로 게임을 설계·구현해 주세요. 세부 구현 방식(프레임워크 사용 여부, 코드 구조)은 당신의 판단에 맡기되,
> **게임 규칙과 판정 로직은 반드시 이 문서의 정의를 따라야 합니다.** (특히 3장 명령어 시스템은 이 게임의 핵심이므로 임의로 단순화하지 마세요.)

---

## 0. 한 줄 컨셉

플레이어는 격자 위를 움직이는 AI 캐릭터에게 **자연어에 가까운 문장으로 명령 프로그램을 작성**한다.
명령이 애매하면(거리·수량·기준이 불명확하면) AI가 엉뚱하게 행동하고, 명령이 프로그램처럼 정확하면 의도대로 정확히 실행된다.
이 과정을 통해 플레이어는 "AI(또는 컴퓨터)에게 지시할 때 왜 구체성이 필요한지"를 퍼즐을 풀며 자연스럽게 체득한다.

---

## 1. 게임 기본 사양

- **시점**: 탑뷰(Top-down) 카메라, 완전한 2D (높이/Y축 개념 없음. 좌표는 (col, row) 2차원만 사용)
- **맵 크기**: 15 × 15 고정 격자
- **캐릭터**: 4방향(상/하/좌/우)만 바라볼 수 있음. 대각선 이동·시선 없음
- **진행 방식**: 스테이지제. 스테이지마다 새로운 맵이 절차적으로 생성됨
- **클리어 조건**: 맵 안의 **열쇠**를 획득하면 해당 스테이지 클리어 → 다음 스테이지로 이동
- **핵심 UI 축**:
  - 좌측/상단: 15×15 격자 맵 뷰
  - 우측/하단: 명령 프로그램 입력창 + 실행 로그(피드백) + 힌트/건너뛰기 버튼

---

## 2. 캐릭터 렌더링 (시야각 표시)

- 캐릭터는 격자 한 칸을 차지하는 아이콘(예: 원형 로봇 모양)으로 표시
- 캐릭터가 "어느 방향을 보고 있는지"를 표현하기 위해, 캐릭터 앞쪽으로 **반투명 부채꼴(시야각 콘) 오버레이**를 표시
  - 구현 방법 제안: SVG `<path>` 부채꼴 또는 CSS `clip-path` 삼각형/사다리꼴을 캐릭터 중심에서 바라보는 방향으로 `rotate` 적용
  - 색상은 반투명(예: 노란색 30% 불투명도)으로, 맵 타일이 살짝 비쳐 보이게
- 캐릭터가 좌/우회전 명령을 실행하면 캐릭터 스프라이트와 시야각 콘이 함께 90도 회전 애니메이션(즉각 전환이 아니라 짧은 트랜지션 권장)
- **중요한 규칙(플레이어에게 사전 고지할 것)**: "왼쪽/오른쪽"은 항상 **캐릭터가 현재 바라보는 방향 기준**이다. 화면(플레이어) 기준이 아니다.
  - 이 규칙은 애매함 판정과 무관하게 게임 시작 시 튜토리얼/가이드로 명확히 안내해야 함. 몰라서 틀리는 것과, 모호하게 말해서 틀리는 것은 반드시 구분해야 게임이 억울하지 않다.

---

## 3. 명령어 시스템 (게임의 핵심)

### 3.1 설계 철학 (반드시 지킬 것)

1. 명령 실패는 항상 **예측 가능한 규칙**을 따라야 한다. 완전 무작위 오작동은 학습 효과가 없으므로 지양한다.
2. 모호한 표현으로 실패했을 때는 반드시 **"왜 실패했는지" 설명하는 피드백 로그**를 함께 보여준다.
3. 게임 자체의 고정 규칙(캐릭터 기준 좌/우, 좌표계 등)은 사전에 명확히 안내하여 "규칙을 몰라서 생기는 실패"를 배제하고, "모호하게 말해서 생기는 실패"만 학습 대상으로 남긴다.
4. 플레이어는 **명령 프로그램(여러 줄)을 한 번에 작성하고 "실행" 버튼으로 전체 실행**하는 것을 기본으로 한다 (아래 3.3 참고). 실시간으로 한 줄씩 개입할 수 없다는 점이 "완전하고 정확한 지시"의 중요성을 체감시킨다.

### 3.2 정확한 명령 문법 (Grammar)

아래 문법에 정확히 매칭되는 문장만 "정확한 명령"으로 인정하고 의도대로 실행한다. (한국어 자연어처럼 보이지만 실제로는 제한된 패턴 집합 — 일종의 텍스트 기반 비주얼 프로그래밍 언어라고 생각하면 됨)

```
<프로그램>   ::= <문장> ( ("." | 줄바꿈) <문장> )*

<문장>       ::= <행동> | <조건문> | <반복문>

<행동>       ::= [숫자 "칸"] ("전진해" | "이동해")
              | [숫자 "칸"] "후진해"
              | "왼쪽으로 돌아" | "오른쪽으로 돌아" | "좌회전해" | "우회전해"
              | "뒤로 돌아" | "180도 돌아"
              | "정지해" | "멈춰"

<센서조건>   ::= ("정면" | "왼쪽" | "오른쪽" | "뒤") "에"
                 ("벽" | "박스" | "구덩이" | "톱날" | "장애물" | "열쇠" | "스위치")
                 ("이" | "가") ("있으면" | "없으면")

<조건문>     ::= ["만약"] <센서조건> <행동>

<반복문>     ::= <행동> "을" 숫자 "번" "반복해"
              | <센서조건 부정형> "때까지" <행동> "을" "반복해"
                (예: "정면에 벽이 없을 때까지 전진해를 반복해")
```

**정확한 명령 예시 (이 게임의 "정답"에 해당하는 문장들)**
- `3칸 전진해.`
- `정면에 벽이 있으면 오른쪽으로 돌아.`
- `정면에 벽이 없을 때까지 전진해를 반복해.`
- `정면에 박스가 있으면 전진해.` *(박스를 향해 전진 = 미는 행동으로 자동 처리)*

이 문법에 정확히 맞으면 → **항상 결정론적으로, 플레이어 의도대로** 실행되어야 한다. (운으로 실패하는 일이 없어야 함)

### 3.3 실행 방식

- 플레이어는 여러 줄로 이루어진 "명령 프로그램"을 텍스트 입력창에 작성
- "실행" 버튼을 누르면 한 줄씩 순서대로 시뮬레이션되며, 캐릭터가 격자 위에서 애니메이션으로 이동/회전
- 실행 중 로그 패널에 각 줄이 어떻게 해석·실행되었는지 실시간으로 표시 (성공/모호함 경고/이해 불가 등)
- 톱날 등 이동 장애물은 캐릭터가 행동(전진/후진/회전/정지)을 1회 실행할 때마다 1틱씩 함께 움직인다 (자세한 내용은 4.4)
- 실행 중 캐릭터가 톱날에 닿거나 구덩이(메워지지 않은)에 빠지면 즉시 "실패" 처리 → 같은 스테이지를 같은 배치로 재시도 가능 (프로그램 텍스트는 유지되어 플레이어가 바로 수정 가능)
- 튜토리얼 성격의 초반 1~2개 스테이지는 "한 줄 입력 → 즉시 실행 → 결과 확인 → 다음 줄 입력" 방식도 허용해 문법 학습 부담을 낮추는 것을 권장

### 3.4 모호함 판별 & 오작동 매핑표 (가장 중요한 부분)

입력한 문장이 3.2 문법에 **정확히 매칭되지 않을 경우**, 아래 표의 "감지 키워드"를 기준으로 모호함 유형을 분류해 정해진 오작동을 실행한다. 어떤 유형에도 해당하지 않으면 "명령을 이해하지 못했습니다"로 처리하고 해당 줄은 무시(패스)한다.

| 모호함 유형 | 예시 표현 | 감지 키워드(예시) | 게임 내 반응(오작동) | 피드백 로그 메시지(예시) |
|---|---|---|---|---|
| 거리/타이밍 불명확 | "벽이 가까워지면 돌아" | 가까워지면, 다가가면, 근처에서, 앞두고 | 무작위 1~3칸 지점에서 반응하거나, 이미 부딪힌 뒤에 반응 | "'가까워지면'은 정확한 거리가 아니에요. 몇 칸 남았을 때인지 말해주세요. 예: '벽과 1칸 남으면'" |
| 모호한 수량 | "조금 가", "여러 번 반복해" | 조금, 약간, 여러 번, 몇 번, 좀 | 무작위 횟수(1~4회)만 실행하고 정지 | "정확한 숫자를 알려주세요. 예: '2칸', '3번'" |
| 주관적 표현 | "빨리 가", "적당히 돌아", "알아서 피해" | 빨리, 천천히, 적당히, 알아서, 조심히, 잘 | 실행되지 않고 제자리에서 대기(무행동) | "AI는 '적당히', '알아서'가 어느 정도인지 몰라요. 구체적인 행동을 알려주세요." |
| 지시대명사 모호 | "그쪽으로 가", "저기로 옮겨" | 그쪽, 저기, 거기, 이쪽 | 무작위 방향으로 회전 또는 이동 | "'그쪽', '저기' 대신 명확한 방향(왼쪽/오른쪽/앞/뒤)을 말해주세요." |
| 조건만 있고 행동 없음 | "벽을 만나면..." | (조건 뒤 매칭되는 행동 없음) | 조건이 충족돼도 아무 행동 안 함 | "조건 다음에 어떤 행동을 할지가 빠졌어요." |
| 반복 종료조건 없음 | "계속 전진해", "끝까지 반복해" | 계속, 끝없이, 무한히, 계속해서 | 벽/장애물에 부딪힐 때까지 실행 후 경고와 함께 정지 | "언제까지 반복할지 조건이 없으면 위험해요. 예: '벽에 부딪힐 때까지 전진해'" |

- 위 표는 최소 시작 세트이며, 개발 중 유형을 자유롭게 추가해도 좋다. 단 **새 유형을 추가할 때도 반드시 "예측 가능한 반응 + 설명 피드백" 쌍을 함께 정의**할 것.
- 방향 기준 혼동("이쪽 말고 저쪽" 등)은 모호함 유형이 아니라 2장에서 정의한 고정 규칙(캐릭터 기준)으로 사전에 해소되므로 별도 오작동 없이 그대로 캐릭터 기준으로 처리한다.

### 3.5 예시 시나리오 (사용자 원본 요청 반영)

- 입력: `앞으로 걸어가다 벽이 가까워지면 오른쪽을 돌아`
  → "가까워지면"이 거리/타이밍 불명확 유형으로 감지 → 캐릭터가 벽에 딱 붙거나 혹은 미리 엉뚱한 지점에서 회전 → 로그에 "정확한 거리를 말해주세요" 피드백 출력
- 입력: `정면에 벽이 있으면 오른쪽으로 돌아. 정면에 벽이 없을 때까지 전진해를 반복해.`
  → 문법에 정확히 매칭 → 항상 의도대로 정확히 실행

---

## 4. 맵 오브젝트 사양

| 오브젝트 | 이동 가능? | 특징 |
|---|---|---|
| 빈 바닥 | - | 자유롭게 이동 가능 |
| 벽 | 불가 | 밀 수 없음. 캐릭터/박스 모두 통과·이동 불가 |
| 박스 | 밀기로 이동 | 캐릭터가 박스를 향해 전진하면 박스가 한 칸 밀림. 박스 뒤가 벽/다른 박스/구덩이(메워지지 않은 채로 존재)가 아니면 이동 성공 |
| 구덩이 | - | 캐릭터가 빠지면 실패(재시도). **박스를 밀어 구덩이에 넣으면 구덩이가 메워지고 그 칸은 평범한 바닥이 된다** (박스는 소모됨) |
| 톱날 | 자동(고정 트랙 왕복) | 아래 4.4 참고 |
| 스위치 | - | 캐릭터가 스위치 칸에 서면(또는 밟으면) 연결된 문이 열리거나 연결된 구덩이가 메워짐 |
| 문 | 스위치로만 개방 | 기본적으로 잠겨 있어 통과 불가. 연결된 스위치가 눌리면 열려서 통과 가능해짐 |
| 열쇠 | 획득 | 캐릭터가 도달하면 스테이지 클리어 |

### 4.1 톱날 상세

- 가로 또는 세로 한 축으로만 왕복 이동 (트랙 양 끝은 벽이나 지정된 좌표)
- **틱(tick) 단위**: 캐릭터가 행동(전진/후진/회전/정지 등)을 1회 실행할 때마다 모든 톱날이 트랙을 따라 1칸씩 이동(끝에 닿으면 반대 방향으로 반전)
- 캐릭터가 톱날과 같은 칸에 있게 되는 순간 실패 처리
- `정면에 톱날이 있으면 정지해` 같은 센서 조건으로 타이밍을 맞춰 회피하는 것이 톱날 스테이지의 핵심 퍼즐
- **v1 범위 제한 권장**: 톱날 트랙과 박스/구덩이 퍼즐 구간은 서로 겹치지 않게 생성해 상호작용 케이스(박스가 톱날에 밀리는 등)를 단순화한다. 추후 확장 가능.

### 4.2 스위치·문 (선택적 요소)

- 모든 스테이지에 필수는 아님. 난이도 곡선에 따라 중반 이후 등장
- 스위치 1개 : 문(또는 구덩이) 1개의 1:1 연결을 기본으로 함
- 스위치의 효과는 둘 중 하나로 랜덤 결정: (a) 지정된 문이 열림 (b) 지정된 구덩이가 메워짐(다리 생성)

---

## 5. 절차적 스테이지 생성 & "무조건 클리어 가능" 보장

### 5.1 생성 알고리즘 (제안 순서)

1. 15×15 전체를 "벽"으로 채운다.
2. 시작 위치(캐릭터 스폰)와 목표 위치(열쇠)를 정한다.
3. 랜덤 워크 또는 재귀적 백트래킹 방식으로 시작→목표를 잇는 **보장된 통로 하나**를 뚫는다 (통로 타일 = 빈 바닥).
4. 난이도 설정에 따라 통로 일부 구간을 다음 중 하나로 변형한다:
   - 박스로 막고 밀 수 있는 여유 공간을 옆에 마련 (박스 퍼즐)
   - 구덩이 + 근처에 밀어 넣을 박스 배치 (구덩이 메우기 퍼즐)
   - 갈림길을 추가로 뚫어 스위치를 배치하고, 원래 통로에는 문을 배치 (스위치 우회 퍼즐)
   - 통로 구간에 톱날 왕복 트랙 배치 (단, 5.2 검증에서 반드시 안전한 타이밍이 존재함을 확인)
5. 남은 벽 공간 일부는 막다른 길이나 가짜 갈림길로 랜덤하게 뚫어 탐색 재미를 더한다 (선택적).
6. 5.2의 솔버로 검증 → 실패 시 처음부터 재생성 (재시도 횟수 초과 시 요소 단순화 후 재시도).

### 5.2 솔버(자동 검증기) 설계

- **상태 정의**: (캐릭터 위치, 캐릭터 방향, 각 박스들의 위치, 각 스위치/문의 상태, 턴 카운트 mod 톱날 주기)
- **행동**: 전진, 후진, 좌회전, 우회전 (밀기는 전진에 자동 포함)
- BFS 또는 DFS로 시작 상태에서 "열쇠 획득" 상태까지 도달 가능한 경로가 존재하는지 탐색
- 상태공간을 감당 가능한 수준으로 유지하기 위해 스테이지당 박스 수(1~3개), 톱날 수(0~3개), 스위치-문 쌍(0~1개) 등에 상한을 둘 것
- 도달 가능하면 스테이지 확정 + **해답 행동 시퀀스를 저장**(힌트 시스템에서 재사용)
- 도달 불가능하면 재생성

### 5.3 난이도 곡선 (요소 등장 가이드, 예시)

| 스테이지 구간 | 새로 등장하는 요소 | 학습 목표 |
|---|---|---|
| 1~2 | 벽만 존재 | 기본 이동/회전 명령 문법 익히기 (고정 튜토리얼 배치 권장, 랜덤 생성 아님) |
| 3~5 | 박스 | 조건문("정면에 박스가 있으면 전진해") 학습 |
| 6~8 | 구덩이 | 박스로 구덩이 메우기, 순서 계획 |
| 9~11 | 스위치·문 | 우회 경로 탐색, 목표까지의 간접 경로 계획 |
| 12~14 | 톱날 | 반복문 + 타이밍 조건("정면에 톱날이 없을 때까지 정지해") |
| 15+ | 복합 스테이지 | 모든 요소 결합 |

전체 스테이지 수는 15~20개 내외를 권장하되 자유롭게 조정 가능.

---

## 6. 힌트 시스템

솔버가 이미 계산해 둔 정답 행동 시퀀스(5.2)를 활용해 3단계로 제공한다.

1. **1단계 (전략 힌트)**: 스테이지에 어떤 요소가 있는지 기반으로 한 일반적 조언. 예: "박스를 이용해 구덩이를 메워야 할 것 같아요."
2. **2단계 (방향 힌트)**: 해답 시퀀스의 다음 1~2스텝을 맵 위에 화살표로 시각 표시
3. **3단계 (정답 명령 힌트)**: 해답 시퀀스를 3.2 문법 형태의 실제 명령 문장으로 변환해 1줄 제공

힌트는 단계별로 순차 공개(1단계 확인 후에만 2단계 열람 가능 등)하며, 사용 여부/횟수를 통계에 기록하는 것을 권장(점수 차감 등은 선택 사항).

---

## 7. 스테이지 건너뛰기 & 진행 관리

- 언제든 "건너뛰기" 버튼으로 다음 스테이지로 이동 가능 (횟수 제한 없음 권장)
- 단, "클리어"와 "건너뛰기"는 통계상 구분하여 기록 (예: 총 클리어 수, 건너뛴 수, 모호한 명령으로 실패한 횟수, 힌트 사용 횟수 등을 진행 화면에 표시)
- 실패 시 재시도는 같은 스테이지 레이아웃 유지(공정성), 원한다면 "새로 생성" 옵션도 별도 제공 가능

---

## 8. 기술 구현 가이드

- 순수 HTML/CSS/JavaScript로 구현 (프레임워크 불필요, 필요하다면 자유롭게 선택)
- 맵 렌더링: CSS Grid로 15×15 셀을 구성하는 방식을 권장 (성능 문제 없음)
- 캐릭터/시야각 콘: SVG 또는 CSS `clip-path` + `transform: rotate()`
- 명령 파서: 정규식 기반 패턴 매칭 + 모호함 키워드 사전(3.4 표)을 별도 모듈로 분리해 유지보수 용이하게 구현
- 솔버: BFS, 상태를 문자열 키로 해싱(JSON.stringify 등)해 방문 체크. 메인 스레드에서 충분히 처리 가능한 규모로 상한을 둘 것(5.2 참고)

---

## 9. 개발 우선순위 (MVP 로드맵)

1. 15×15 격자 렌더링 + 캐릭터 이동/회전 + 시야각 표시 + 벽 충돌
2. 기본 이동/회전 명령 파서 + 실행 애니메이션
3. 조건문 파서 + 모호함 감지·오작동 매핑(표의 2~3종부터 시작) + 피드백 로그
4. 박스 밀기 + 구덩이 메우기
5. 스위치·문
6. 톱날 + 타이밍 로직
7. 절차적 생성 알고리즘 + 솔버 검증 (5장)
8. 힌트 시스템 + 스테이지 건너뛰기
9. 반복문 지원 + 모호함 유형 확장
10. 고정 튜토리얼 스테이지(1~2개) + 통계/스코어 UI 폴리싱

---

## 10. 최종 지시사항

위 사양에 따라 완결된 하나의 웹 게임을 만들어 주세요. 특히 **3장(명령어 시스템)과 5장(생성·검증)은 이 게임의 정체성**이므로 절대 임의로 생략하거나 지나치게 단순화하지 말아 주세요. 나머지 세부 디자인(색상, 폰트, 애니메이션 디테일 등)은 자유롭게 재량껏 정해도 좋습니다.
